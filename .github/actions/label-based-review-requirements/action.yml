name: 'Review Requirements'
description: 'Enforces review requirements based on the applied labels.'
runs:
  using: 'node20'
  main: 'dist/index.js'
  token:
    description: GitHub Access Token. Will be used to fetch PR reviews.
    required: true